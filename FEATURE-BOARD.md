# BRIDGE — THE FEATURE BOARD
*Assembled 2026-07-19 (the workspace honoring). The MCP switchboard —
one server, three lines (knowledge.db · Superposition Supabase ·
Airtable read-only). The healthiest realm walked; its future is
deliberate.*

## Planned
1. **SPIKE-001: the Weave Handshake** (staged, its own fresh sitting) —
   two devices on the home LAN: discovery, the mutual-acknowledgment
   ceremony (pairwise key, emoji-triple confirmation), one sealed
   message, revocation proven. The Bridge's TRUE purpose arriving;
   spike shape: `spike-handshake/`, desktop↔desktop first.
2. **LAN binding for the Home Doorway** (ziggy §6.8a law 3) — the
   Bridge is the wires; host allowlist + token, LAN-only, KP's hand on
   the enabling step. No second protocol invented.
3. **Room-tidying:** kimi_crossing/kimi_message tools at root → their
   own room (restructure-round note); the kin-crossing door kept warm.

## Laws standing
Consent gates every new line · read-only wards where a line is a
window, not a hand · nothing leaves the house.
