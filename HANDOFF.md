# HANDOFF — resonance-bridge

*Where this realm stands, right now. **Regenerated whole** at each close that
worked here — never appended to. Git holds every prior day.*

*This is not `docs/CHECKLIST.md`. The checklist is the append-only ledger of
what was **done**; this sheet is the state of what **stands**. It cites the
checklist and never restates it. **Ceiling 8 KB** — a sheet past it is
over-written and re-cut, never rolled. The law:
[THE-ROLL](https://github.com/Quantum-Weaver/resonance-standards/blob/main/docs/THE-ROLL.md).*

```
LEGEND   state  ● done · ◐ drifted/partial · ⏸ his hold · — not yet
         hand   ⚛ his word · ✋ his hands · 🕯️ a lamp's
         proof  ✔ gates green · ✗ failing · ? unproven
```

**resonance-bridge** · 2026-08-22 · last hand **Tiento 🎻 (Fable, claude-fable-5[1m])** · branch `main` · **0 ahead / 0 behind** · working tree **6 paths** — 4 modified · 2 untracked, 1 of them trees (`media/` 10).
*Machine-read 00:36 by `handoff_tender.py` — **no build gate was run**; any gate figure below keeps its own date and `?`. Last commit `1e2e095` **tender run**, Quantum Weaver 2026-08-21 15:48.*

## Where it stands

*The switchboard: one MCP server, nine line modules, fifty-four tools on two doors. Read
from the ground 2026-08-22 00:3x — git · `FEATURE-BOARD.md` 1–11 · `docs/CHECKLIST.md`
(born 08-21) · `CLAUDE.md` · `resonance-weaver/static/{movies,games}/meta.json`.*

| what | state | hand | proof |
|---|---|---|---|
| Nine lines under `src/lines/` (airtable · discord · family · github · grammar · resend · stripe · supabase · vercel), **54 tools**, two doors — stdio `src/server.ts` · HTTP `src/http.ts` :3141 for the Shuttle (by hand, no npm script) | ● | 🕯️ | ✔ grep `server.tool(` = 54, 08-22 · smoke last whole **08-15** (`FEATURE-BOARD.md` 8) ? |
| **The media lane** — `media/_common.py` · `screen_lander.py` · `games_lander.py`, caches + overrides beside them; reads only the Well's `consumed-media/`; lands into `resonance-weaver/static/movies/` + `static/games/` (`npm run movies` · `npm run games` there) | ● | 🕯️ Tiento, at his ⚛ word | ✔ landed 08-22 05:21 / 05:23 Z per `meta.json` — 411/447 matched · 410 posters (tmdb w185) · IGDB 284 exact / 32 best / 26 none · 313 covers; files on disk 371 posters · 302 covers, counted 00:3x |
| `.env.example` +5 names — `TWITCH_CLIENT_ID` / `_SECRET` · `TMDB_READ_ACCESS_TOKEN` · `TMDB_API_KEY` · `IMDB_DATASETS_DIR`; values on the ring by his hand, never opened here | ● | ✋ | ✔ `git diff .env.example` 08-22; the runs answered live |
| Battle.net — `battlenet_character.py` built, **stands unused** (his character + realm names); `battlenet_collections.py` landed live into weaver `static/blizzard/` with `--enrich` | ● | 🕯️ · ✋ names | ? last run **08-16**, `FEATURE-BOARD.md` 9–10 |
| Family line `src/lines/family.ts` — 4 read-only windows; `beacons` the true name since seed 096 (dual-resolve kept, truth first) | ● | 🕯️ | ✔ grep 4, 08-22 · live **08-15** (`FEATURE-BOARD.md` 8) ? |
| DeepSeek room — `deepseek_message.py` live 08-14; `deepseek_crossing.py` **HELD SHUT**, his own knock | ⏸ | ✋ | `deepseek/GUEST.md` · Pratīkṣaṇa H-b / O-a |
| `docs/CHECKLIST.md` born 08-21 beside `FEATURE-BOARD.md`; **`CLAUDE.md:20` still says the realm keeps none** — which door is the door | ◐ | ⚛ | `docs/CHECKLIST.md:3–9` · THE-BRIDGE-BOARD §Drift flags |
| Planned, unbuilt — SPIKE-001 the Weave Handshake · LAN binding for the Home Doorway · Meta/Facebook read-only line (TJ's yes first) | — | ⚛ | `FEATURE-BOARD.md` §Planned 1–3 · `docs/CHECKLIST.md` Phase 7 |
| Realm board `THE-BRIDGE-BOARD.html` trued 08-21 15:43 · `docs/blueprints/bridge/` forged 08-21 15:19 (`4d7fdab`) — both predate `media/` | ◐ | `rack-tender` · the forge | mtime · git, 08-22 |
| `package.json` `license: ISC` vs `LICENSE` MIT | ◐ | ✋ | `docs/CHECKLIST.md` B2 |

## What waits, and whose

| what waits | whose | source |
|---|---|---|
| The sync word — 4 modified (`.env.example` · `FEATURE-BOARD.md` · `HANDS.md`, Tiento's seat signed 00:34 · `docs/CHECKLIST.md`) + `media/` untracked (9 files git sees; `__pycache__` ignored) + this sheet; weaver's `static/movies/` · `static/games/` · `package.json` ride beside, in that realm | ⚛ | `git status --porcelain` 08-22 · `docs/CHECKLIST.md` 08-22 row |
| His eye on `media/review-fuzzy.csv` (the fuzzy candidates) and the 36 unmatched; `imdb-overrides.csv` / `igdb-overrides.json` are his hand's to fill | ✋ | `FEATURE-BOARD.md` 11 · `media/review-fuzzy.csv` |
| Which door is the door — `CLAUDE.md:20` *"keeps no CHECKLIST"* vs `docs/CHECKLIST.md`; and `CLAUDE.md:42` *"Nineteen python instruments"* vs 5 at the root | ⚛ | `docs/CHECKLIST.md:3–9` · THE-BRIDGE-BOARD §Drift flags |
| Character + realm names for the Battle.net character line | ✋ | `FEATURE-BOARD.md` 9 |
| The DeepSeek crossing — his own knock, never automated | ✋ | `deepseek/GUEST.md` · `desk/records/the-pratiksana/REGISTER.md:172` (H-b · O-a) |
| SPIKE-001 and the LAN binding — each its own fresh sitting at his word; end states *unwritten — his to rule* | ⚛ | `FEATURE-BOARD.md` §Planned 1–2 |
| The Meta/Facebook line — TJ's yes is the door, for the export and for the line; nothing of hers pulled before it | ⚛ (TJ's consent first) | `FEATURE-BOARD.md` §Planned 3 |
| `package.json` license field — ISC or MIT | ✋ | `docs/CHECKLIST.md` B2 |

## Read before you touch this repo

- **No standing process** — the client births the server each session; "restart the server" is restart the session; `npm run register` lights the wick; `npm run smoke` clean before commit (`CLAUDE.md` §Ground rules).
- **Read-only everywhere; keys by name off the ring, never printed** — grep every landed file, cache and script for the secret strings after a run; the media lane reads **only** `consumed-media/` in the Well, never sealed/ or health/ (`FEATURE-BOARD.md` 10–11).
- **LANDED FILES are the only bus into an app** — weaver consumes `static/movies|games|blizzard/`, never the Bridge at runtime; caches beside the scripts make re-runs near-instant (`FEATURE-BOARD.md` 10–11).
- **Two lessons the first runs taught** — an exact-title hit with a disagreeing year outranks a fuzzy guess at another title; the storefront's `PS4 & PS5` tails are never part of a work's name (`FEATURE-BOARD.md` 11).
- **The base leads, the bridge follows** — names change in the Grammar first (seeds 096/097), then here; `family.ts` dual-resolve stays, truth first (`FEATURE-BOARD.md` 8).
- **One detail call per unique thing, never per instance**; transmog sets stay name-only (`FEATURE-BOARD.md` 10, amendment).

---

*No truth of its own. The ledger is `docs/CHECKLIST.md` (beside `FEATURE-BOARD.md`, the
realm's first-person record); the voices are `HANDS.md`; what waits is
`resonance-chamber/desk/THE-PRATIKSANA.html`; the index is
`resonance-chamber/path/handoffs/THE-HANDOFF-RACK.md`. **Where this sheet and the ground
disagree, the ground is right** — regenerate it.*

— strip regenerated by `handoff_tender.py` at the hand of **Tiento 🎻 (Fable, claude-fable-5[1m])**, 2026-08-22 00:36. **No build gate was run** — every gate figure above keeps the date it was measured. The voiced sections are carried verbatim and belong to the hands that wrote them.
