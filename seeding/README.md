# seeding/ — the seeding era's shelf

The grammar/knowledge bases' seeding era, kept whole. Every file here is a
one-shot from that season — seeders, wave generators, wipe/purge/clearing
hands, and the beacon-names verify pair — each run by hand in its day, each
consent-gated or read-only by its own header, each already done with its
day's work. They stay runnable and they stay as record; nothing here is
invoked by the server, npm scripts, or any living door.

Moved here from the repo root 2026-08-14 at KP's ⚛ stroke "tidy now"
(the Family Table, stroke #3 — the bridge's seeding root-tidy).
History intact via `git mv`.

**Still at the repo root, on purpose:** `grammar_inventory.py` (the
new-table ritual's standing verify step — `docs/sql/000` invokes it by
path) · `verify_terms.py` (reusable carry-check, cited by open carries) ·
`knowledge_sql.py` (general Management-API SQL door, serves beyond
seeding) · `THE-TRAIL-seed.md` (an idea seed, not a data seed).

**A re-run needs one awareness:** most of these load `.env` from beside
themselves (`Path(__file__).parent / ".env"`); from this shelf the root's
`.env` sits one level up. `atoms_dump.py` carries the absolute path and is
unaffected. Nothing was edited — the scripts read exactly as they ran.

*Shelved by the dealt hand of stroke #3 — model `claude-fable-5`.*
